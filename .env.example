POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=job_market
POSTGRES_USER=job_market
POSTGRES_PASSWORD=changeme

# Optional: enables the AI Career Insight on the salary predictor. Without this
# set, prediction works exactly the same via the from-scratch NumPy models.
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
