# payscope
Salary prediction built on ML models written from scratch in NumPy (no scikit-learn) — cleans messy real-world job postings, predicts pay from role/skills/location, and explains each number with an optional Groq AI agent.
